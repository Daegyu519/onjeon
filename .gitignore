.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.DS_Store
.streamlit/secrets.toml
.env
data/qdrant/
data/cache.db
.run/
# 실물 등기부 등 개인정보 문서 — 공개 리포 유출 방지.
# **디렉터리 통째로** 막는다. `*.pdf`만으로는 부족하다 — OCR 폴백(함정 7·12)이
# 촬영본·스캔본을 다루므로 .jpg/.png/.heic/덤프 .txt가 같은 폴더에 떨어진다.
# 확장자로 막으면 그 순간 공개 저장소에 소유자 성명이 실린다.
data/registers_raw/
data/fixtures/real_registers/
*.pdf

# WAL 모드(cache.py open_cache) 사이드카 — 런타임 산물, 커밋 대상 아님
data/cache.db-wal
data/cache.db-shm
.gstack/

# 공모전 제안서 초안 — 로컬 전용. 공개 저장소라 의도적으로 추적하지 않는다.
KB_AI_Challenge_제안서_초안.md

# 문서 빌드 산출물(docx·pdf·html) — docs/summary.md와 diagrams/에서 재생성된다.
dist-docs/

# 기술설명서 표지의 팀 정보 — 실명·학교라 커밋하지 않는다(공개 저장소).
# build_deck.py가 빌드할 때만 표지 사본(.build-*.html)에 주입하고 곧바로 지운다.
docs/deck/team.local.json
docs/deck/.build-*.html
