# TODOS

작업 대기 항목. 각 항목은 3개월 뒤에 읽어도 배경·현재상태·시작점을 알 수 있게 적는다.

---

## 1. 실제 등기부 샘플 10건 L1 정확도 표

**배경:** L1 파서를 검증한 근거가 `data/fixtures/`의 매물 2건과 `fake_registers`의
합성 등기부뿐이다. CLAUDE.md 함정 4가 정확히 이 구멍에서 나왔다 — 가짜 등기부에
'주요 등기사항 요약' 절이 없어서 테스트 12개가 전부 통과하는 동안 채권최고액이
2배로 계산되고 있었다. **픽스처가 실제 형식과 다르면 테스트는 아무것도 보증하지 않는다.**

**현재상태:** 파서는 집합건물·건물 등기부, 텍스트 레이어·스캔본(OCR) 4가지 경로를
다룬다. 필드별로 실패 시 예외 대신 `None` + 사유를 남긴다. 하지만 **실제 발급본으로
측정한 필드별 정확도 숫자가 없다.**

**시작점:** 실제 등기부 10건(집합건물 5·건물 3·스캔본 2) 확보 → 주소·전용면적·
채권최고액·용도 4필드의 정답을 손으로 적어 `data/fixtures/`에 넣고 →
`tests/test_register_*.py`에 정확도 표 테스트 추가. 데이터가 먼저다.

---

## 2. 임대차 중개보수를 전세·월세 비용에 넣기

**배경:** 매수안은 중개보수를 비용에 넣는데(공인중개사법 시행규칙 별표 1 매매 구간)
전세·월세는 넣지 않는다. 별표 1에는 임대차 구간이 따로 있다(1억~6억 1천분의 3,
5천만~1억 1천분의 4·한도 30만원).

**현재상태:** 룰(`rules/tax_rules_2026-07.json` → `brokerage.limitation`)에 빠진
사실과 실측 크기를 적어 뒀다. 전세 2억 ≈ 60만원/2년, 월세(보증금 2천만 + 차임
55만×100 = 7,700만) ≈ 30만원/2년 → **연 15만원가량 전세에 불리한 방향**이 빠져 있다.
전세·월세 양쪽에 함께 붙어 상당 부분 상쇄되지만 0은 아니다.

**시작점:** `brokerage`에 `lease_brackets`를 추가(원문은 이미
`rules/law_clauses_2026-07.json`의 `공인중개사법 시행규칙 > tables > 별표 1`에 있다) →
`engine.bracket_fee`를 그대로 재사용 → `annual_cost_jeonse`/`annual_cost_wolse`에
`(중개보수 / 계약주기 2년)` 항 추가 → 화면 breakdown 행 추가. 거래금액 계산 규칙은
시행규칙 §20⑤1호(보증금 + 월차임×100, 5천만 미만이면 ×70)를 따라야 한다.

---

## 3. 매수대출 금리를 실측으로

**배경:** 전세대출 금리는 HF 공개 API(15082044) 15개 은행 금액가중평균으로 실측했다
(3.676%). 주택담보대출 금리 4.5%는 **대표값으로 고른 판단값**이다.

**현재상태:** `rules/market_params_2026-07.json`의 `loan_rate_buy_judgment`에
판단값임과 오차 방향(어느 쪽인지 모른다)을 명시했다. 매수는 참고 비교로만 쓰고
결론(전세 vs 월세)에는 들어가지 않는다.

**시작점:** `scripts/fetch_bank_rates.py`에 주담대 데이터셋 추가. HF 데이터 목록에
전세대출 외 상품이 있는지 먼저 확인하고, 없으면 은행연합회 공시를 본다.

---

## 4. 보유세를 개산율에서 구간 누진으로

**배경:** 재산세를 시세 대비 단일 개산율 0.15%로 근사한다. 실제는 과세표준 구간
누진이고(지방세법 §111①3호나목) 1세대1주택 특례세율(§111의2)이 따로 있다.

**현재상태:** 조문 원문·유도 과정·판단 링크를 `tax_rules_2026-07.json`의
`holding`에 다 적었다. 유도값 0.131% 대비 0.15%는 약 15% 크게 잡혀 있어
**매수안을 불리하게** 만드는 방향(보수적)이다. 사슬에서 법령이 아닌 것은
`시가표준액 ÷ 시세 ≈ 0.7` 하나다.

**시작점:** 실거래가로 시세를 알고 있으니 공시가격만 있으면 정확해진다 —
부동산공시가격 알리미 API 또는 공동주택 공시가격 데이터셋 확인. 그 전에는
개산율 유지가 맞다(없는 정밀도를 만들지 않는다).

---

## 5. 월세 세액공제 §95-2 ② (배우자 추가공제) + 근로소득 여부

**배경:** 조특법 §95조의2 제2항이 2025-12-23 신설됐다 — 요건을 갖춘 배우자도
추가로 공제받을 수 있고 부부 합계 1,000만원 한도다. 또 조문은 **근로소득**을
요구하는데 소득 유형을 수집하지 않는다.

**현재상태:** `wolse_tax_credit`은 무주택·세대주·총급여 상한·연 1,000만원 한도까지
반영한다. 위 두 개는 `limitation`에 적혀 있고 미반영이다. 방향은 반대다 —
배우자 미반영은 기혼 맞벌이의 공제를 **과소**(월세가 비싸 보임), 근로소득 미확인은
사업소득자에게 공제를 **과대**(월세가 싸 보임) 계상한다.

**시작점:** 입력 2개 추가(`income_type`, 배우자 요건) → `_Profile` 스키마 →
`engine.wolse_tax_credit` 키워드 인자. 입력을 늘리기 전엔 고칠 수 없다.

---

## 6. 서울 밖 지원

**배경:** 2026-07-28부터 서울 밖 매물은 `/api/decision`이 400으로 **거절한다**.
막은 이유는 두 축이 함께 비기 때문이다 — 실거래가 캐시에 해당 지역 거래가 0건이고,
최우선변제도 시행령 §10·§11의 서울 구간만 반영돼 있다.

**현재상태:** 4구간 전체 금액은 이미 룰에 원문으로 들어 있다
(`market_params_2026-07.json` → `small_deposit_priority.other_regions_reference`).
막힌 것은 **어느 구간인지 판정하는 부분**이다 — 과밀억제권역·광역시 군지역 제외 같은
지역 분류가 필요하고 이건 수도권정비계획법 시행령 별표를 봐야 한다.

**시작점:** (a) 수도권정비계획법 시행령 별표1(과밀억제권역)을
`fetch_law_clauses.py`의 `tables`로 수집 → `region_match`를 4구간으로 확장.
(b) `scripts/warm_cache.py`로 대상 지역 실거래가 캐시 워밍 + `SEOUL_LAWD_CD`를
전국 시군구 코드로 확장. **둘을 같이 해야 한다** — 하나만 하면 지금 막은 이유가
그대로 남는다.

---

## 7. 남은 판단값 3개를 실측으로

**배경:** 세제 4항목은 2026-07-28 법제처 원문 대조로 검증했지만, 아래 3개는
법령 근거가 없는 판단값이다. 전부 `judgment: true` + `error_direction`으로 표시돼 있다.

| 값 | 현재 | 영향 |
|---|---|---|
| 보증금 기회비용 | 4.0% | 전세 보증금에 크게 작용 — ±1%p = 연 ±200만원. **결론 부호를 뒤집을 수 있다** |
| 적정주거비 상한(RIR) | 30% | 진단 문구만 바뀐다. 비용 계산에는 영향 없음 |
| 시세 추정 불확실성 | 지번 10 / 동 20 / 구 30% | 기대손실 밴드 폭. 좁으면 위험을 확신하게 만든다 |
| 전월세전환율 | 5.5% | 시세 차트의 월세 환산 표시 전용 — 결론 계산에는 미사용 |

**시작점:** 기회비용이 가장 급하다(결론을 뒤집는 유일한 값). 다만 '보증금 기회비용의
기준을 무엇으로 볼지'는 데이터가 정해주지 않는 규범적 선택이라, 실측보다 **감도를
화면에 보여주는 쪽**이 정직할 수 있다. 시세 불확실성은 캐시의 지번·동·구별 평당가
분산을 직접 계산하면 바로 실측된다 — `data/cache.db`에 828,383건이 있다.

---

## 완료 기록

- **배포 API에 E[Loss] 배선** (2026-07-27) — `decision.py::_risk()`가 `P × LGD × 보증금`을
  `breakdown["미회수기대손실"]`로 넣는다. sklearn을 배포 경로로 끌고 올 필요가 없었다 —
  로지스틱 회귀 추론은 시그모이드 한 줄이라 계수만 룰 JSON(`rules/risk_model_*`)으로
  빼면 stdlib로 충분하다. `tests/test_risk_wiring.py`가 최상단 import를 정적 검사한다.
- **채권최고액 자동 추출** — `register/parse.py::extract_senior_claims`. 요약 절 앞의
  본문만 센다(함정 4).
- **세제 파라미터 법령 원문 대조** (2026-07-28) — 취득세·보유세·중개보수·월세 세액공제
  4항목. **중개보수 0.5%가 틀린 값이었다**(2억~9억은 별표1에서 1천분의 4). 취득세도
  단일 1.1%가 아니라 3구간이라 6억 초과에서 과소계상하고 있었다(7억 기준 연 128만원 차이).
