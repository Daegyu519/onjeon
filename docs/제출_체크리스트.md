# 제출 체크리스트 — KB Future Finance A.I. Challenge 제8회

> 이 파일은 제출 zip의 최상단 `00_제출_체크리스트.md`로 들어간다.
> 심사위원용이 아니라 **제출하는 우리를 위한 것**이다.

## 마감과 방법 (주최사 사이트 원문 기준)

| 항목 | 내용 |
|---|---|
| **마감** | **2026. 8. 3(월) 16:00** — 이후 추가 접수 불가 |
| 제출 경로 | https://www.kb-aichallenge.com → 참가신청서 제출 |
| 형식 | **ZIP 1개**. 최대 3GB. 파일 선택 시 즉시 업로드가 시작된다 |
| 담는 것 | ① 참가신청서 ② 기술설명서(파워포인트) ③ 프로토타입(구현코드) |
| 본선 발표 | 8. 20(목) 11:00 개별 통보 · 본선 9. 2(수) 이화여자대학교 ECC 이삼봉홀 |

주최사 공지: 마감일에 신청이 몰려 접속 지연이 발생할 수 있고, **파일 업로드에 시간이
걸린다**. 3GB 한도 안이지만 이 zip도 100MB대다 — 여유를 두고 올린다.

---

## 남은 일 — 사람만 할 수 있는 것

### ☐ 1. 기술설명서 표지의 팀 정보 3칸

표지(1쪽)의 **팀명 · 팀원 · 소속**은 원본 HTML에서 점선 공란으로 남겨 뒀다. 이 저장소는
공개돼 있어서 실명·학교를 커밋하면 **git 히스토리에 영구히 남는다.** 그래서 값은
gitignore된 파일 하나에만 적고, 빌드할 때 표지 사본에 주입한다.

`docs/deck/team.local.json`을 새로 만든다 (**이 파일은 커밋되지 않는다**).

```json
{
  "팀명": "○○○",
  "팀원": "○○○(팀장) · ○○○",
  "소속": "○○대학교 ○○학과 · ○○대학교 ○○학과"
}
```

값은 참가신청서 2번 표(`01_참가신청서/…참가신청서.docx`)에 적어 둔 것을 **그대로** 옮긴다 —
두 문서가 다르면 심사자가 어느 쪽을 믿어야 할지 알 수 없다.

그다음 빌드한다. 파일이 있으면 표지에 값이 박히고, 없으면 공란인 채로 나온다
(빌드 로그 첫 줄에 어느 쪽인지 찍힌다). 주입된 사본은 빌드가 끝나면 지워진다.

```bash
.venv/bin/python scripts/build_deck.py     # 24장 재렌더 → dist-docs/*.pptx, *.pdf
./scripts/make_submission.sh               # zip 재생성
```

- ☐ 빌드 로그에 `▶ 표지 팀 정보: team.local.json 주입`이 찍혔는지 확인
- ☐ PDF 1쪽에 세 칸이 채워졌는지 눈으로 확인 (점선이 남아 있으면 JSON 키 오타다)

> PPTX는 네이티브 텍스트라 파워포인트에서 직접 고쳐도 된다. 다만 그렇게 고치면 PDF와
> 갈라진다 — 두 파일이 같아야 하므로 JSON을 고쳐 다시 빌드하는 쪽을 쓴다.

### ☐ 2. 참가신청서 docx 마무리

`01_참가신청서/제8회_Future_Finance_AI_Challenge_참가신청서.docx`에 이미 팀명·과제명·
과제요약·팀원 정보가 들어 있다. 남은 것은 **손으로 체크·서명해야 하는 칸**이다.

- ☐ 2번 표 — 팀장·팀원의 **재학 구분** (재학 / 휴학) 체크
- ☐ 3번 표 — AI 활용 **결과물 반영 정도** (참고 / 일부반영 / 대부분활용) 체크
- ☐ 하단 **날짜**를 제출일로 (현재 `2026년 7월 31일`)
- ☐ 하단 **신청인(팀장) 서명 또는 날인**
- ☐ PDF로 내보내 docx와 함께 넣기 (서명이 들어간 형식이 안전하다)

### ☐ 3. 서약서 — **팀원 각각** 작성·서명

`01_참가신청서/서식_원본(서명전)/제8회 Future Finance AI Challenge 서약서.pdf`

- ☐ 팀장 서명본
- ☐ 팀원 서명본
- ☐ 서명본을 `01_참가신청서/`에 넣고 `서식_원본(서명전)/` 폴더는 zip에서 지운다

### ☐ 4. 개인정보 수집·이용 동의서 — **팀원 각각**

`01_참가신청서/서식_원본(서명전)/[필수]개인정보 수집·이용 동의서(...).pdf`

- ☐ 팀장 서명본
- ☐ 팀원 서명본

> 참가신청서 첨부1이 "참가 서약서 및 개인정보 수집·이용 동의서 1부 **(개인별 작성 필요)**"라고
> 명시한다. 2인 팀이면 각 서식이 2부씩, 총 4장이다.

### ☐ 5. zip 재생성 후 업로드

```bash
./scripts/make_submission.sh
```

- ☐ zip을 풀어 `01_참가신청서/`에 **서명본 4장**이 들어 있는지 눈으로 확인
- ☐ `서식_원본(서명전)/` 폴더가 남아 있지 않은지 확인
- ☐ 사이트 업로드 → 진행률 **100%** 확인 후 「등록하기」 클릭 (100% 전에 누르면 거부된다)

---

## 이미 확인한 것

| 항목 | 상태 |
|---|---|
| 기술설명서 24쪽 (pptx + pdf) | 같은 HTML 원본에서 생성 — pptx는 편집 가능한 네이티브 도형 |
| 문서 실측치 | 2026-08-03 배포 경로 재실행으로 재현 확인 (921/508/+414만원 · E[Loss] 442만원) |
| 테스트 | 개발 환경 **576 passed** / 심사 환경(실물 등기부·선택 의존성 없음) **506 passed, 53 skipped, 실패 0** |
| 개인정보 | 실물 등기부 발급본 3건 zip에서 제외 · 추적 파일 전체 키 스캔 통과 |
| API 키 | `.env` 제외, `.env.example`만 포함 |
| 프로토타입 무키 실행 | `data/cache.db`(실거래 82만건) 포함 — 키 없이 화면이 그대로 뜬다 |

## 주최사 사실관계 (2026-08-02 사이트 확인)

- 주제는 둘 중 택일 — **현직자 Pick** 또는 자유주제. 우리는 현직자 Pick의 **청년 주거**.
  ※ 주제 리스트 원문은 로그인이 필요해 확인하지 못했다. **표지의 주제 문구를 사이트의
  공식 주제명과 대조할 것** (`docs/deck/온전_기술설명서.html`의 `<dt>주제</dt>` 줄).
- 참가자격: 대학·대학원 재(휴)학생(26.6월말 기준 학적 유지) · 개인 또는 최대 3명 ·
  제1~7회 수상자 제외. **출생일 하한이 따로 있다 — 사이트 FAQ에서 연도를 직접 확인할 것**
  (여기 적지 않은 이유: 사이트 스크립트에서 연도가 잘려 확인하지 못했다. 추측을 적으면
  자격 판단을 틀린 근거로 하게 된다).
- 시상: 대상 1팀 1,000만원 / 최우수 2팀 각 600만원 / 우수 2팀 각 300만원.
  우수상 이상은 KB국민은행 신입행원(L1) ICT 지원 시 서류·필기 면제(2027년까지 1회).
- 문의: 운영사무국 (KB국민은행 금융AI1센터 노연희)
